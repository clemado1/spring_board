<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.javalec.spring_board.dao.BDao">
	<select id="findBId" resultType="Integer">
		SELECT MAX(BID) FROM BOARD
	</select>
	
	<select id="findRId" resultType="Integer">
		SELECT MAX(BID) FROM RBOARD
	</select>
	
	<select id="listCount" resultType="Integer">
		SELECT COUNT(*) FROM BOARD
	</select>
	
	<select id="slistCountAll" resultType="Integer">
		SELECT COUNT(*) FROM BOARD WHERE TITLE LIKE CONCAT('%',#{param1},'%') OR CONTENT LIKE CONCAT('%',#{param1},'%')
	</select>
	
	<select id="slistCount" resultType="Integer">
		SELECT COUNT(*) FROM BOARD WHERE #{param1} LIKE CONCAT('%',#{param2},'%')
	</select>
	
	<select id="listDao" resultType="com.javalec.spring_board.dto.BDto">
		SELECT BID, NAME, TITLE, substring(CONTENT,1,50) AS CONTENT, DATE, HIT, BGROUP, BREPLY, BSTEP FROM BOARD ORDER BY BID DESC LIMIT #{param1},5
	</select>
	
	<select id="slistDaoAll" resultType="com.javalec.spring_board.dto.BDto">
		SELECT BID, NAME, TITLE, substring(CONTENT,1,50) AS CONTENT, DATE, HIT, BGROUP, BREPLY, BSTEP FROM BOARD WHERE TITLE LIKE CONCAT('%',#{param1},'%') OR CONTENT LIKE CONCAT('%',#{param1},'%') ORDER BY BID DESC LIMIT #{param2},5
	</select>
	
	<select id="slistDao" resultType="com.javalec.spring_board.dto.BDto">
		SELECT BID, NAME, TITLE, substring(CONTENT,1,50) AS CONTENT, DATE, HIT, BGROUP, BREPLY, BSTEP FROM BOARD WHERE #{param1} LIKE CONCAT('%',#{param2},'%') ORDER BY BID DESC LIMIT #{param3},5
	</select>
	
	<select id="searchList" resultType="com.javalec.spring_board.dto.BDto">
		SELECT BID, NAME, TITLE, substring(CONTENT,1,50), DATE, HIT, BGROUP, BREPLY, BSTEP FROM BOARD ORDER BY BID DESC
	</select>
	
	<select id="viewDao" resultType="com.javalec.spring_board.dto.BDto">
		SELECT * FROM BOARD WHERE BID = #{param1}
	</select>
	
	<select id="viewReply" resultType="com.javalec.spring_board.dto.BDto">
		SELECT * FROM RBOARD WHERE BGROUP = #{param1} ORDER BY HIT DESC, BREPLY ASC, BSTEP ASC 
	</select>
	
	<insert id="writeDao">
		INSERT INTO board VALUES(#{param1}, #{param2}, #{param3}, #{param4}, SYSDATE(), 0, #{param1}, #{param1}, 1)
	</insert>
	
	<insert id="replyDao">
		INSERT INTO RBOARD(BID, NAME, CONTENT, DATE, HIT, BGROUP, BREPLY, BSTEP) VALUES(#{param1}, #{param2}, #{param3}, SYSDATE(), 0, #{param4}, #{param1}, 2)
	</insert>
	
	<insert id="commentDao">
		INSERT INTO RBOARD(BID, NAME, CONTENT, DATE, HIT, BGROUP, BREPLY, BSTEP) VALUES(#{param1}, #{param2}, #{param3}, SYSDATE(), 0, #{param4}, #{param5}, 3)
	</insert>
	
	<update id="upHit">
		UPDATE board SET HIT = HIT + 1 WHERE BID = #{param1}
	</update>
	
	<update id="upReply">
		UPDATE board SET BREPLY = BREPLY + 1 WHERE BID = #{param1}
	</update>
	
	<update id="upRHit">
		UPDATE RBOARD SET HIT = HIT + 1 WHERE BID = #{param1}
	</update>
	
	<update id="modifyDao">
		UPDATE board SET TITLE = #{param2}, CONTENT = #{param3} WHERE bid = #{param1}
	</update>
	
	<delete id="deleteDao">
		DELETE FROM board WHERE bid = #{param1}
	</delete>
	
	<delete id="deleteReply">
		DELETE FROM RBOARD WHERE BGROUP = #{param1}
	</delete>
	
	<!-- user 관련 매핑 -->
	
	<select id="testEmail" resultType="Integer">
		SELECT COUNT(*) FROM USER WHERE EMAIL = #{param1}
	</select>
	
	<select id="testName" resultType="Integer">
		SELECT COUNT(*) FROM USER WHERE NAME = #{param1}
	</select>
	
	<select id="signIn" resultType="Integer">
		SELECT COUNT(*) FROM USER WHERE EMAIL = #{param1} AND PASSWD = #{param2}
	</select>
	
	<select id="userList" resultType="com.javalec.spring_board.dto.UserDto">
		SELECT * FROM USER WHERE EMAIL = #{param1}
	</select>
	
	<insert id="signUp">
		INSERT INTO USER(EMAIL, NAME, PASSWD, UDATE, ENABLED) VALUES(#{param1}, #{param2}, #{param3}, SYSDATE(), 1)
	</insert>
	
	<insert id="signUpRole">
		INSERT INTO USER_ROLE(EMAIL) VALUES(#{param1})
	</insert>
	
	<update id="modName">
		UPDATE USER SET NAME = #{param2) WHERE UID = #{param1}
	</update>
	
	<update id="modPass">
		UPDATE USER SET PASSWD = #{param2) WHERE UID = #{param1}
	</update>
	
	<delete id="deleteUser">
		DELETE FROM USER WHERE UID = #{param1}
	</delete>
	
</mapper>
